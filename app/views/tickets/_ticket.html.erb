<tr>
  <td class="col-sm-4"><%= link_to ticket.name, ticket_path(ticket) %></td>
  <td class="col-sm-2">Assignee</td>
  <td class="col-sm-2"><%= ticket.status %></td>
  <td class="col-sm-2"><%= parse_tags(ticket.tags) %></td>
  <td class="col-sm-2"><%= link_to 'Edit', edit_ticket_path(ticket) %> <%= link_to 'Delete', ticket_path(ticket), method: :delete, data: { confirm: "Are you sure?" } %></td>
</tr>